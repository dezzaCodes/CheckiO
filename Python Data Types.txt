String
* text.split()
   * Separates sentence into words array
   * split(' ') = split around one space
* range(a, b)
* [::-1]
   * Reverse string
* [:-1]
   * Cut off last part of string
* len(str)
* s.isupper()
* s.isnumeric()
* s.isspace()
* max(iterable)
* lower()
* upper()
* isalpha()
* t.index(element)
* s.endswith(str)
* s.replace(a, b)
   * Replace all instances of a with b
* s.capitalize()
   * Capitalise first letter of word




Arrays
* a.index(element)
* a.append(element)

Set
* s.add(element)


Datetime
* 

Dictionaries
* sorted(data, key=lambda x: x['price'], reverse=True)
   * Sort based on key 'price' in descending order